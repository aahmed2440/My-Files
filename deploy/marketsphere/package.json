{
  "name": "marketsphere-certification-runtime",
  "version": "4.6.0-evidence",
  "private": true,
  "description": "MarketSphere application-certification runtime with append-only hashed evidence, readiness, source state, owner RBAC, recovery proof, and certification bundles.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": { "node": ">=22" },
  "license": "UNLICENSED"
}
